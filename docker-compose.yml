services:
  magneto-api:
    build:
      context: .
      dockerfile: dockerfile.local
    ports:
      - "8001:8001"
    volumes:
      - .:/home/magnetoapp/api
    environment:
      - PYTHONUNBUFFERED=1
